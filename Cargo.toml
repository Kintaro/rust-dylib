[project]
name = "dylib"
version = "0.1.0"
authors = ["Simon Wollwage"]
license = "BSD-3-Clause"
description = "Standalone version of former dylib module"

[dependencies]
libc = "0.2"

[lib]

name = "dylib"
crate-type = ["rlib"]
